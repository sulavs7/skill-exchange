
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign Up</title>
  <link rel="stylesheet" href="stylesheets/signup-tourist.css">
</head>
<body>
  <!-- Modal background -->
  <div class="modal-background" id="modalBackground" onclick="redirectToLoginPage()">
    <div class="modal" onclick="event.stopPropagation()">
      <button class="close-btn" onclick="closeModal()">×</button>
      
      <h1>Sign Up as Tourist</h1>
      <form action="/signup" method="POST">
        <div class="form-group">
          <label for="email">Email:</label>
          <input type="email" id="email" name="email" required>
        </div>
        <div class="form-group">
          <label for="password">Password:</label>
          <input type="password" id="password" name="password" required>
        </div>
        <!-- Add a hidden field or a selection for the role -->
        <input type="hidden" name="role" value="tourist"> <!-- or set to "local" based on context -->
        <button type="submit">Sign Up</button>
      </form>
      
    </div>
  </div>

  <script>
    // Function to close the modal
    function closeModal() {
      document.getElementById('modalBackground').style.display = 'none';
    }

    // Redirect to the login page when clicking outside of the modal
    function redirectToLoginPage() {
      window.location.href = '/login'; // Change this to your login route
    }

    // Prevent modal from closing when clicking inside the modal
    document.querySelector('.modal').addEventListener('click', function(e) {
      e.stopPropagation();
    });
  </script>
</body>
</html>
